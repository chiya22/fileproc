<!DOCTYPE html>
<html>

<head>
  <link rel="stylesheet" href="/stylesheets/destyle.css" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1, user-scalable=yes">
  <title>パスワード変更</title>
</head>

<body>
  <div class="main-container">
    <% if (flashMessages) { %>
      <% if (flashMessages.msg) { %>
        <div class="alert">
          <p class="alert-danger"><%= flashMessages.msg %></p>
        </div>
      <% } %>
    <% } %>
    <form name="userform" action="/changepwd" method="POST">
      <div class="form-area">
        <div><input class="form-item-readonly-style" type="text" name="id" placeholder="ID" value="<%= selectuser.id %>"></div>
        <div><input class="form-item-readonly-style" type="text" name="name" placeholder="名前" value="<%= selectuser.name %>"></div>
        <div><input class="form-item-style" type="password" name="password" placeholder="パスワード" value="" autofocus required></div>
        <input type="hidden" name="role" value="<%= selectuser.role  %>">
        <input type="hidden" name="ymd_start" value="<%= selectuser.ymd_start %>">
        <input type="hidden" name="ymd_end" value="<%= selectuser.ymd_end %>">
        <input type="hidden" name="ymd_add" value="<%= selectuser.ymd_add %>">
        <input type="hidden" name="id_add" value="<%= selectuser.id_add %>">
        <input type="hidden" name="ymd_upd" value="<%= selectuser.ymd_upd %>">
        <input type="hidden" name="id_upd" value="<%= selectuser.id_upd %>">
        <input class="btn-update" type="submit" id="btn_update" value="更新">
      </div>
    </form>
    <div class="btn-footer">
      <a href="/" class="btn-link">戻る</a>
      <% if (locals.user){ %>
        <form method="POST" action="/logout">
          <input type="submit" class="btn-link" button value="ログアウト" />
        </form>
      <% }  %>
    </div>
  </div>
</body>

</html>